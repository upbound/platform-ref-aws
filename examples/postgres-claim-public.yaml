apiVersion: aws.platformref.crossplane.io/v1alpha1
kind: PostgreSQLInstance
metadata:
  name: my-db
  namespace: default
spec:
  parameters:
    storageGB: 20
    networkRef:
      name: network
    connectivity: public
  writeConnectionSecretToRef:
    name: db-conn
